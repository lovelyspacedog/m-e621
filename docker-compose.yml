services:
  app:
    build: .
    ports:
      - "18621:18621"
    environment:
      M_E621_DOMAIN: localhost
      # Optional host-wide FurAffinity session (do not commit real cookies):
      # FA_COOKIE_A: ""
      # FA_COOKIE_B: ""
    volumes:
      - me621-config:/data/config

volumes:
  me621-config:
